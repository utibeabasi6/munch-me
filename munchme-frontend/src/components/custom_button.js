import { Button } from 'semantic-ui-react';

export function CustomButton(props) {
    return <Button {...props} style={{ padding: '15 20', borderRadius: 0, backgroundColor: "#f0f8ff", color: "#333" }} />

}