import { Button } from 'semantic-ui-react';

export function CustomButton(props) {
    return <Button {...props} style={{marginTop: 20, padding: 15, borderRadius: 0, backgroundColor: "#f0f8ff", color: "#333"}}/>
     
}